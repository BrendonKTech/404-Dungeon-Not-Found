price = 50;
hovered = false;

// Fixed base scale (for hitbox)
base_scale = 1;

// Animated scale for smooth effect
current_scale = base_scale;

// Initialize "Not enough gold" message variables
not_enough_gold_text = "";
not_enough_gold_timer = 0;

success_text = "";
success_timer = 0;