/*if (instance_exists(player)) {
    draw_text(50, 50, "HP: " + string(player.hp) + "/" + string(player.max_hp));
    draw_text(50, 70, "MP: " + string(player.mp) + "/" + string(player.max_mp));
}

if (instance_exists(enemy)) {
    draw_text(400, 50, "Enemy HP: " + string(enemy.hp) + "/" + string(enemy.max_hp));
}
