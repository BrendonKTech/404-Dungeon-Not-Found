/*index_clicked = -1;

reward_cards = [];

hover_index = -1;

if (index_clicked == -1) {
    for (var i = 0; i < array_length(reward_cards); i++) {
        draw_sprite(reward_cards[i].sprite_index, 0, 200 + i*200, 300);
    }
    return;
}
