// Start at a node (first node on the map)
/*current_node = instance_find(obj_map_node, 0);

// Move player to current node position
x = current_node.x_pos;
y = current_node.y_pos;

// Player can only move to one of the next_nodes array
target_node = noone;
